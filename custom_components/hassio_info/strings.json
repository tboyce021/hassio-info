{
  "config": {
    "flow_title": "Home Assistant Supervisor+",
	"step": {
      "user": {
        "title": "Confirm set up"
      }
    },
    "abort": {
      "single_instance_allowed": "[%key:common::config_flow::abort::single_instance_allowed%]",
	  "hassio_required": "The core Supervisor integration is not set up"
    }
  }
}